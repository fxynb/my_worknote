// ie7以上
xhr = new XMLHttpRequest()

xhr.open() //接收三个参数  第一个参数是请求类型 get post 第二个接收请求的url地址，第三个是否异步发送请求的布尔值

//这只是针对请求url地址的请求类型的一个请求，调用open并不会真的发送请求，而是启动请求以备发送，要发送必须使用send方法

xhr.send() //接收一个参数，作为请求主体发送的数据，如果不需要则发送为null